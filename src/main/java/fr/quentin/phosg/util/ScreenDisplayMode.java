package fr.quentin.phosg.util;

/**
 * Enum representing different screen display modes.
 */
public enum ScreenDisplayMode {
    FADE,       // Fade out elements when necessary
    OVERLAY,    // Keep elements overlapping
    MOVE        // Move elements to accommodate rendering
}
